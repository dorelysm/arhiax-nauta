{
  "name": "arhiax-nauta",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test runtime/atk/*.test.mjs",
    "fixtures": "node scripts/test-fixtures.mjs",
    "schema:emit": "node scripts/emit-schemas.mjs",
    "sbom": "cyclonedx-npm --output-format JSON --output-file docs/sbom.json --omit dev"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-npm": "^4.2.1"
  }
}
